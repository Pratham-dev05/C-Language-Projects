#include<stdio.h>

int main()
{
    int a=10, b=3;

    printf("AND Operator (a&&b): %d\n", a&&b);
    printf("OR Operator (a||b): %d\n", a||b);
    printf("NOT Operator !(a==b): %d\n", !(a==b));

    return 0;
}